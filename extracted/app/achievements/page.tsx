import {site} from "@/data/site"; import AwardCard from "@/components/AwardCard"; export default function Achievements(){return <main className="section"><div className="wrap"><div className="eyebrow">Achievements</div><h1 className="title">Proof of the journey.</h1><p className="text">Awards and recognition — tap "See details" for the full story.</p><div style={{marginTop:30}}>{site.achievements.map(a=><AwardCard a={a} key={a.n}/>)}</div></div></main>}
