import {site} from "@/data/site"; export default function Life(){return <main className="section"><div className="wrap two"><div><div className="eyebrow">06 / Life & Journey</div><h1 className="title">The person behind the projects.</h1></div><div><div className="eyebrow">Education</div><div className="rows" style={{marginTop:15}}>{site.education.map((e,i)=><div className="row" key={i}><div className="num">{e.year}</div><div><h2 style={{margin:0}}>{e.degree}</h2><p className="text">{e.place}<br/>{e.text}</p></div></div>)}</div><div style={{marginTop:70}}><div className="eyebrow">Skills & tools</div><div className="skillgrid" style={{marginTop:25}}>{Object.entries(site.skills).map(([k,v])=><div key={k}><h3>{k}</h3><div className="tags">{v.map(x=><span className="tag" key={x}>{x}</span>)}</div></div>)}</div></div></div></div></main>}