import Link from "next/link"; import {ArrowUpRight} from "lucide-react"; import {site} from "@/data/site"; export default function Projects(){return <main className="section"><div className="wrap"><div className="eyebrow">02 / Projects</div><h1 className="title">Things I built.</h1><p className="text">A growing archive of experiments, prototypes and systems.</p><div className="cards" style={{marginTop:50}}>{site.projects.map(p=><article className="card" key={p.id}><img className="thumb" src={p.image} alt={p.title}/><div className="cardbody"><div className="eyebrow">{p.year} · {p.type}</div><h2>{p.title}</h2><p className="text">{p.description}</p><div className="tags">{p.tags.map(t=><span className="tag" key={t}>{t}</span>)}</div><Link className="btn" style={{marginTop:22}} href={"/projects/"+p.id}>Open project <ArrowUpRight size={14}/></Link></div></article>)}</div></div></main>}