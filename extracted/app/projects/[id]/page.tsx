import {notFound} from "next/navigation"; import {site} from "@/data/site"; import Link from "next/link"; export default async function Project({params}:{params:Promise<{id:string}>}){const{id}=await params;const p=site.projects.find(x=>x.id===id);if(!p)return notFound();return <main className="section"><div className="wrap"><Link href="/projects" className="eyebrow">← Back to projects</Link><div className="two" style={{marginTop:35}}><img className="thumb" style={{borderRadius:24}} src={p.image} alt={p.title}/><div><div className="eyebrow">{p.year} · {p.type}</div><h1 className="title">{p.title}</h1><p className="text">{p.details}</p><div className="tags">{p.tags.map(t=><span className="tag" key={t}>{t}</span>)}</div></div></div></div></main>}