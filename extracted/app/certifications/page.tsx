import {site} from "@/data/site"; export default function Certifications(){return <main className="section"><div className="wrap"><div className="eyebrow">Certifications</div><h1 className="title">Credentials.</h1>{site.certifications.length===0?<div className="emptybox" style={{marginTop:40}}>No certifications added yet.</div>:<div className="rows" style={{marginTop:50}}>{site.certifications.map((c:any,i:number)=><article className="row" key={i}><div className="num">{String(i+1).padStart(2,"0")}</div><div><h2 style={{margin:"0 0 7px"}}>{c.title}</h2><div className="eyebrow">{c.org} · {c.date}</div></div>{c.link&&<a className="result" href={c.link}>Verify</a>}</article>)}</div>}</div></main>}
